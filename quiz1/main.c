// Author: 
#include <stdio.h>
#include <readline.h>
#include "quiz1.h"

int main(void) {
  n = int(readline("Enter an int: "))
  base = int(readline("Enter an int: "))
  printf("answer is %f", digit_sum_base(n, base))
  return 0;
}
